{
  "build": "public",
  "baseurl": "test-theme/",
  "routes": {
    "404": "/:filename",
    "index": "/:paginate(totalPages)",
    "pages": "/pages/:filename",
    "posts": "/posts/:filename",
    "projs": "/projs/:paginate(totalProjPages)"
  },
  "local": {
    "port": 3000
  }
}
