{
  "build": "public",
  "baseurl": "/",
  "routes": {
    "index": "/:paginate(totalPages)",
    "pages": "/pages/:filename",
    "posts": "/posts/:filename",
    "projects": "/:filename/:paginate(totalProjPages)",
    "404": "/:filename"
  },
  "local": {
    "port": 3000
  }
}
