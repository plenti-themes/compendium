/* generated by Svelte v3.38.3 */
import { SvelteComponent, init, safe_not_equal } from '../web_modules/svelte/internal/index.mjs';

function geo() {
	// Use fetch method to GET ip data for request options
	const address = fetch("https://jsonip.com", { mode: "cors" }).then(resp => resp.json()).then(data => {
		return data.ip;
	});

	return address;
}

class Component extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, null, safe_not_equal, {});
	}
}

export default Component;
export { geo };